{% extends 'BackofficeBaseBundle::Layout/AdminLayout.html.twig' %}

{% block contentHeader %}
<h1>
    Administration des etablissements
</h1>
{% endblock %}

{% block content %}
<div class="col-md-12 col-sm-12 col-xs-12">
    <div class="box">
        <div class="box-header">
            <h3 class="box-title">Liste des établissements</h3>
        </div>
        <div class="box-body">
            <table class="table table-bordered table-striped dataTable">
                <thead>
                    <tr>
                        <th>Id</th>
                        <th>Nom</th>
                        <th>Adresse</th>
                        <th>Ville</th>
                        <th>Codepostal</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tfoot>
                    <tr>
                        <th>Id</th>
                        <th>Nom</th>
                        <th>Adresse</th>
                        <th>Ville</th>
                        <th>Codepostal</th>
                        <th>Actions</th>
                    </tr>
                </tfoot>
                <tbody>
                    {% for lieux in lieuxes %}
                    <tr>
                        <td><a href="{{ path('lieux_show', { 'id': lieux.id }) }}">{{ lieux.id }}</a></td>
                        <td>{{ lieux.nom }}</td>
                        <td>{{ lieux.adresse }}</td>
                        <td>{{ lieux.ville }}</td>
                        <td>{{ lieux.codePostal }}</td>
                        <td>
                            <ul>
                                <li>
                                    <a href="{{ path('lieux_show', { 'id': lieux.id }) }}">show</a>
                                </li>
                                <li>
                                    <a href="{{ path('lieux_edit', { 'id': lieux.id }) }}">edit</a>
                                </li>
                            </ul>
                        </td>
                    </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>

    <ul>
        <li>
            <a href="{{ path('lieux_new') }}">Create a new lieux</a>
        </li>
    </ul>
{% endblock %}
